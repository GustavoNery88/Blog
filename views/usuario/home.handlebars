<!DOCTYPE html>
<html lang="pt-br">

<head>
</head>

<body>
  <div class="row mb-4 altura">
    {{#each postagens}}
    <div class="col-md-6">
      <div class=" row no-gutters border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative m-1">
        <div class="col p-4 d-flex flex-column position-static">
          <strong class="d-inline-block mb-2 text-primary"></strong>
          <h3 class="mb-0">{{titulo}}</h3>
          <div class="mb-1 text-muted"></div>
          <p class="card-text mb-auto">{{descricao}}</p>
          <a href="{{slug}}" class="stretched-link">Leia mais...</a>
        </div>
        <div class="col-auto d-none d-lg-block p-3">
          <img src="/uploads/{{imagem}}" width="200" height="200" alt="">
        </div>
      </div>
    </div>

    {{else}}


    {{/each}}
  </div>
  </div>
</body>

</html>